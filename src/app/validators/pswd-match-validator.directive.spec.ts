import { PswdMatchValidatorDirective } from './pswd-match-validator.directive';

describe('PswdMatchValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PswdMatchValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
